{
  "name": "jpv-nexus-firebase-validation",
  "private": true,
  "scripts": {
    "test": "node --test test/rules.test.mjs",
    "test:emulators": "firebase emulators:exec --only auth,firestore,storage \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.0",
    "firebase-tools": "14.20.0"
  }
}
